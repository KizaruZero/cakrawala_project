<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!--        Inherited the Res Config Settings to add new field on it-->
    <record id="res_config_settings_view_form_purchase" model="ir.ui.view">
        <field name="name">
            res.config.settings.view.form.inherit.purchase.down.payment
        </field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="95"/>
        <field name="inherit_id" ref="purchase.res_config_settings_view_form_purchase"/>
        <field name="arch" type="xml">
            <xpath expr="//block[@name='invoicing_settings_container']" position="inside">
                <setting help="Product used for down payments in purchase bill">
                    <field name="po_deposit_default_product_id"/>
                </setting>
                <setting help="Product used for Biaya Admin Leasing in DP Invoice (Leasing PO)">
                    <field name="leasing_admin_product_id"/>
                </setting>
                <setting help="Product used for AP Leasing-Angsuran Pertama in DP Invoice (Leasing PO)">
                    <field name="leasing_ap_product_id"/>
                </setting>
            </xpath>
        </field>
    </record>
</odoo>
